package chap_07.Sec5.Package2;

import chap_07.Sec5.Package1.A;
public class C {
    /*
    // 패키지를 임포트 하였더라도 A와 다른 패키지에 있기 때문에 protected 를 사용할 수 없다.
    public method(){
        A a = new A();
        a.field = "value";
        a.method();
    }
     */
}
